# Lab 1

[Begin Lab 1: Preparing the Network Automation Workstation](Lab1.md)

Supporting files are stored in the [`files`](files/) directory. Copy `.env.example` to `.env` only when instructed by the lab, and never commit the populated `.env` file.
